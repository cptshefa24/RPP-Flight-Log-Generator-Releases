# RPP Flight Log Generator — Releases

Public, binary-only update channel for **RPP Flight Log Generator by Simon S**.

This repository contains only approved Windows release assets and update metadata:

- `RPP_Flight_Log_Generator.exe`
- `latest.json`

The application source remains private in `cptshefa24/RPP-Flight-Log-Generator`.

## Separation

This update channel is exclusively for the RPP Flight Log Generator. It is not connected to Shefa Financials and must never contain financial source code, installers, secrets, data, tags, or release assets.

Do not manually edit `latest.json`; the private RPP build workflow creates it with the release version and SHA-256 checksum.
